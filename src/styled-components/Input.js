import styled from "styled-components";

const Input = styled.input`
    width: 60%;
    font-family: inherit;
    font-size: 1.1rem;
    padding: .5rem;
    margin: 1rem 0;

    ::placeholder {
        color: black;
        font-style: italic
    }
`

export default Input;